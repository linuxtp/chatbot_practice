
First start fronend with below command:
  >> cd chatapp_frontend
  >> npm run dev
  
Second start backend with below command:
  >> cd chatapp_backend
  >> node .\index.js
  

On browser go to http://localhost:3000
 -> enter query in first field
 -> mention a pdf file name in second field
 -> click submit to get response